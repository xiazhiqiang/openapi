{
  "name": "generator",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm run clean && ag ./apis/ws.yml @asyncapi/nodejs-ws-template -o example -p server=localhost --force-write",
    "clean": "rm -rf example/*",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@asyncapi/generator": "^1.10.1",
    "@asyncapi/nodejs-template": "^0.13.2",
    "@asyncapi/nodejs-ws-template": "^0.9.33"
  }
}
